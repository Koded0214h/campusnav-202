# Copy to .env.local and fill in your own token: https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=

# CampusNav Spring Boot API base URL. Defaults to http://localhost:8080
# (matching `mvn spring-boot:run`) when unset — only override for a
# deployed backend.
VITE_API_BASE_URL=
